/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['static.iceaestofados.com.br'], // Adicione o domínio do seu host de imagem aqui
      },
}

module.exports = nextConfig
